import { Feed } from '@src/components';
import React from 'react';
import { SafeAreaView } from 'react-native';

const Home: React.FC = () => (
  <SafeAreaView>
    <Feed />
  </SafeAreaView>
);

export default Home;
